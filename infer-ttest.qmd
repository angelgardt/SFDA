# Сравнение средних {#infer-ttest}

```{r, echo=FALSE, message=FALSE, error=FALSE, warning=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE, error = FALSE)

library(tidyverse)
theme_set(theme_bw())
```

В предыдущей главе мы рассмотрели статистические методы, позволяющие выяснить, есть ли взаимосвязь между двумя категориальными переменными. Однако более частой задачей является изучение взаимосвязи между *категориальной* и *количественной* переменной. В частности, в рамках эксперимента у нас, как правило, есть как минимум экспериментальная и контрольная группы --- категориальная переменная `группа` --- и мы хотим выяснить, различаются ли значения количественной зависимой переменной между этими группами --- иначе, связана принадлежность к экспериментальной или контрольной группе с тем, какие значения принимает зависимая переменная. Другая ситуация --- неэкспериментальное исследование, когда группы формируются по какому-либо признаку --- год обучения, специальность образования, уровень дохода, профессиональная сфера и др. При этом не столь принципиально, как именно сформированы группы --- нас всё равно могут интересовать различия между группами. Займёмся изучением подходящих под изучение этого дела статистических методов.



## Гипотезы «о различиях» и «гипотезы о связях» {#ttest-hyps}

Часто при обучении статистике, да и в практике, любят разделять гипотезы «о различия [между группами]» и гипотезы «о связях [между переменными]». Отчасти это верно, так как в одних исследованиях мы технически сравниваем группы друг с другом (экспериментальная и контрольная, бакалавры и магистранты, линейные сотрудники и руководители), а в других ищем взаимосвязи между переменными (личностные черты и удовлетворенность жизнью, опыт работы и приверженность компании, социальные верования и доверие к политическим институтам).

Однако по факту мы можем свести все гипотезы к гипотезам о связях --- разница лишь в том, связи между какими переменными (по типу) мы ищем. Если это связи между *количественными* переменными, то это будут «классические» гипотезы о связях, если же это связи между *количественными и категориальными* переменными, то они будут эквивалентны гипотезам о различиях между группами по категориальной переменной.

> Возможно, сейчас не вполне понятная ценность такого обобщения, но она нам пригодится, когда мы будем рассматривать *линейные модели* как класс статистических методов.



## «Сравнение групп» vs «сравнение средних» {#ttest-groupmeans}

Итак, мы отважились сравнивать группы друг с другом --- но как это сделать? Есть много статистик, которые описывают группы --- среднее, медиана, мода, дисперсия, стандартное отклонение, асимметрия, эксцесс… Тем не менее, чаще всего сравнение групп осуществляют через *сравнение средних* этих групп.

Хотя это и не обязательно --- можно, в принципе, сравнить группы, используя медианы --- среднее имеет несколько преимуществ:

- *статистическое преимущество*: не вдаваясь в математические детали, отметим, что выборочное среднее является хорошей[^mean-good-estimate] генерального среднего, гипотезы относительно которого мы и формулируем.
- *прикладное преимущество*: статистические модели для сравнения средних достаточно просты и их результаты довольно легко интерпретируемы в рамках прикладных исследований.
- *практическое преимущество*: среднее имеет естественную интерпретацию для многих практических задыч и измерений, а сравнение средних логично отвечает на многие исследовательские вопросы.

[^mean-good-estimate]: На статистическом --- несмещенной, состоятельной и эффективной.

Ключевые общие вещи обсудили, пора уже приступить к сути дела.



## Одновыборочный t-тест {#ttest-onesample}

Первым тестом, с которым мы познакомимся в рамках сравнения средних, станет одновыборочный t-тест. Это может вызвать некоторое замешательство, поскольку выше мы говорили о сравнение средних в двух выборках. Спокойно --- до этого тоже дойдем. Хоть одновыборочный t-тест и не так часто встречается как самостоятельный статистический тест, он является частью статистических методов, которые мы будем изучать далее, а также позволит нам ещё раз напомнить себе, как вообще устроено статистическое тестирование гипотез.

**Какую исследовательскую задачу мы будем решать?** Пусть мы HR некоторой компании и занимаемся мониторингом выгорания сотрудников. Нынче конце квартала, и вчера вечером нам от руководства упала задача на изучение выгорания сотрудников двух ключевых отделов --- маркетинг и разработка. Опустим сейчас детали планирования исследования и подбора психометрических методик, и обозначим, что мы провели скрининговый замер уровня выгорания сотрудников с помощью опросника на профессиональное выгорание. Данных за предыдущие недели/месяцы у нас нет --- придется выкручиваться.

Мы хотим выяснить, **превышает ли в среднем уровень выгорания** сотрудников обоих отделов **«нормальный» уровень выгорания**. Под «нормальным» уровнем выгорания мы будем иметь в виду статистические нормы по опроснику. Для определенности скажем, что результаты переводились в стандартную 100-балльную шкалу ($\mathcal N(50, 100)$), следовательно, верхней границей «нормального» уровня выгорания ($\mu + \sigma$) будет $60$.

Имеем следующие данные --- аггрегированные показатели (@tbl-onesample-data). Дополнительно визуализируем распределения переменных (@fig-onesample-data).

:::{#tbl-onesample-data}
```{r}
set.seed(123)
n_dev <- 27
n_market <- 34
mean_dev <- 63
mean_market <- 58
sd_dev <- 7
sd_market <- 12

dev <- rnorm(n = n_dev, mean = mean_dev, sd = sd_dev)
market <- rnorm(n = n_market, mean = mean_market, sd = sd_market)

# t.test(dev, alternative = "greater", mu = 60)
# t.test(market, alternative = "greater", mu = 60)

tibble(
  "Отдел" = c("Разработка", "Маркетинг"),
  "Количество респондентов" = c(length(dev), length(market)),
  "Среднее" = c(mean(dev), mean(market)),
  "Срандартное отклонение" = c(sd(dev), sd(market))
) %>% knitr::kable(digits = 2)
```

Аггрегированные данные об уровне профессионального выгорания сотрудников
:::

:::{#fig-onesample-data}

```{r}
tibble("Разработка" = c(dev, rep(NA, n_market - n_dev)),
       "Маркетинг" = market) %>% 
  pivot_longer(cols = everything()) %>% 
ggplot() +
  geom_histogram(aes(value),
                 binwidth = 1) +
  geom_vline(xintercept = 60, linetype = "dashed") +
  facet_wrap(~name, ncol = 1) +
  labs(x = "Балл по опроснику выгорания",
       y = "Количество")
```

Распределение баллов по опроснику профессионального выгорания у сотрудников двух отделов
:::

Ситуация неоднозначная --- нужно тестировать.

Как завещано (@nte-nhst2), первоначально *формулируем гипотезы*. В общем виде интересующую нас гипотезу можно сформулировать так:

$$
\begin{split}
H_0: \mu = \mu_0 \\
H_1: \mu \neq \mu_0
\end{split}
$$

То есть, словесное оформление нулевой гипотезы --- *среднее генеральной совокупности равно некоторому заданному значению*. Альтернативной, соответственно --- *среднее генеральной совокупности не равно некоторому заданному значению*.

Так как нам известен допустиный уровень выгорания ($\mu_0 = 60$ в нашем случае) и поскольку нас интересует превышение этого уровня, сформулируем сразу *направленные* гипотезы:

$$
\begin{split}
H_0: \mu \leq \mu_0 \\
H_1: \mu > \mu_0
\end{split} \quad 
\text{или} \quad
\begin{split}
H_0: \mu \leq 60 \\
H_1: \mu > 60
\end{split}
$$

Уровень значимости оставим конвенциональным $\alpha = 0.05$.

Для тестирования такой гипотезы подойдет **t-критерий Стьюдента**, предложенный в 1908 году У. Госсетом. Расчёт статистики критерия выглядит так:

$$
t = \frac{\overline x - \mu_0}{s / \sqrt{n}} = \frac{\overline x - \mu_0}{\text{se}_X} \overset{H_0}\thicksim t(n-1)
$${#eq-onesample}

Разберемся сначала со смыслом его расчета:

- в числителе стоит разница между выборочным средним и заданным значением --- это и есть сравнение двух чисел;
- в знаменателей стоит стандартная ошибка среднего --- это учёт вариативности данных.

Мы можем рассчитать наблюдаемые значения t-статистик для наших групп:

- для маркетинга: $t_{\text{obs}} = \dfrac{60.12 - 60}{10.05 / \sqrt{34}} \approx 0.068$
- для разработки: $t_{\text{obs}} = \dfrac{62.56 - 60}{6.86 / \sqrt{27}} \approx 1.941$

Чудненько. Теперь необходимо оценить, насколько эти значения типичны для случая, когда верна нулевая гипотеза. Следовательно, необходимо распределения статистики критерия, справедливое для случая, когда верна нулевая гипотеза. Как написано выше (@eq-onesample), статистика t-критерия при справедливости нулевой гипотезы подчиняется t-распределению (распределению Стьюдента), с параметром $n-1$. Выглядит оно так (@fig-tdist). Параметр $n-1$ --- это степени свободы, определяющие форму распределения.

:::{#fig-tdist}
```{r}
ggplot() +
  geom_function(aes(color = "1"), fun = dt, args = list(df = 1)) +
  geom_function(aes(color = "2"), fun = dt, args = list(df = 2)) +
  geom_function(aes(color = "5"), fun = dt, args = list(df = 5)) +
  geom_function(aes(color = "10"), fun = dt, args = list(df = 10)) +
  geom_function(aes(color = "15"), fun = dt, args = list(df = 15)) +
  geom_function(aes(color = "20"), fun = dt, args = list(df = 20)) +
  geom_function(aes(color = "25"), fun = dt, args = list(df = 25)) +
  geom_function(aes(color = "30"), fun = dt, args = list(df = 30)) +
  scale_x_continuous(limits = c(-3.5, 3.5)) +
  scale_color_discrete(name = "Degrees of Freedom") +
  theme(legend.position = "bottom") +
  labs(x = "t", y = "Density")
```
Распределению Стьюдента при различном количестве степеней свободы
:::

В нашем случае мы будем работать с двумя конкретными распределениями, имея следующие ситуации (@fig-onesample-res).

:::{#fig-onesample-res}
```{r}
#| layout-ncol: 2
#| label: fig-onesample-res
#| fig-subcap: 
#| - "Отдел маркетинга"
#| - "Отдел разработки"

t_obs_mark <- t.test(market, mu = 60, alternative = "greate")$statistic
t_obs_dev <- t.test(dev, mu = 60, alternative = "greate")$statistic

res_mark <- 
  ggplot() +
  geom_function(fun = dt, args = list(df = n_market-1)) +
  stat_function(fun = dt, args = list(df = n_market-1),
                xlim = c(qt(p = 1 - .05, df = n_market-1), 3.5), 
                geom = "area", fill = "red", alpha = .5) +
  stat_function(fun = dt, args = list(df = n_market-1),
                xlim = c(t_obs_mark, 3.5), 
                geom = "area", fill = "blue", alpha = .5) +
  geom_vline(xintercept = qt(p = 1 - .05, df = n_market-1), linetype = "dotted") +
  geom_vline(xintercept = t_obs_mark,
             linetype = "dashed") +
  annotate(geom = "text", label = latex2exp::TeX("$t_{obs}$"),
           x = 2.2, y = .1) +
  xlim(-3.5, 3.5) +
  labs(x = "t", y = "Density")

res_dev <- 
  ggplot() +
  geom_function(fun = dt, args = list(df = n_dev-1)) +
  stat_function(fun = dt, args = list(df = n_dev-1),
                xlim = c(qt(p = 1 - .05, df = n_dev-1), 3.5), 
                geom = "area", fill = "red", alpha = .5) +
  stat_function(fun = dt, args = list(df = n_dev-1),
                xlim = c(t_obs_dev, 3.5), 
                geom = "area", fill = "blue", alpha = .5) +
  geom_vline(xintercept = qt(p = 1 - .05, df = n_dev-1), linetype = "dotted") +
  geom_vline(xintercept = t_obs_dev,
             linetype = "dashed") +
  annotate(geom = "text", label = latex2exp::TeX("$t_{obs}$"),
           x = 2.2, y = .1) +
  xlim(-3.5, 3.5) +
  labs(x = "t", y = "Density")


print(res_mark)
print(res_dev)
```
Результаты тестирования статистических гипотез для обеих выборок (@tbl-onesample-data)
:::

Согласно алгоритму статистического вывода:

- для отдела маркетинга (@fig-onesample-res-1) имеем $\mathsf{p} > \alpha$, следовательно, мы получил *типичное* значение критерия для случая, кода верна нулевая гипотеза, поэтому у нас *нет оснований* отклонять нулевую гипотезу $\mu = 60$.
- для отдела разработки (@fig-onesample-res-2) имеем $\mathsf{p} < \alpha$, следовательно, мы получил *нетипичное* значение критерия для случая, кода верна нулевая гипотеза, поэтому у нас *есть основания* отклонить нулевую гипотезу в пользу альтернативной, заключив, что мы получили свидетельство того, что в среднем уровень выгорания команды разработки превышает «нормальный» (допустимый).


### Размер эффекта для одновыборочного t-теста {#ttest-onesample-effectsize}

Итак, для группы разработки мы обнаружили эффект --- получили статистически значимые результат. Хотелось бы узнать, насколько этот эффект велик, ведь по абсолютным значениями выборочное среднее (`r mean(dev) %>% round(2)`) превышает 60 всего на пару баллов. Для этого необходимо рассчитать меру размер эффекта, которой для t-теста является **d Коэна (Cohen's d)**.

$$
d = \frac{\overline x - \mu_0}{s_X}
$$

В нашем случае для разработчиков $d = \dfrac{62.56 - 60}{6.86} \approx 0.37$. Это много или мало?

Сам Д. Коэн предложил такие пороги для интерпретации (@tbl-cohensd). Таким образом, можно сказать, что мы обнаружили эффект, близкий к среднему.

:::{#tbl-cohensd}

|Величина|Значение|
|:---|--:|
|Малый|0.2|
|Средний|0.5|
|Большой|0.8|

Интерпретация размера эффекта Cohen's d
:::



## Двухвыборочный t-тест {#ttest-twosample}

### Для независимых выборок {#ttest-twosample-independent}

$$
t = \frac{
  \overline x_1 - \overline x_2
  }{
  \sqrt{
    \dfrac{s^2_1}{n_1} + \dfrac{s^2_2}{n_2}
}} \overset{H_0}{\thicksim} t(\text{df})
$$



:::{.callout-note appearance="minimal" collapse=true title="Какая-то страшная формула"}

$$
\text{df} = \frac{
  \left( \dfrac{s_1^2}{n_1} + \dfrac{s_2^2}{n_2} \right)^2
}{
  \dfrac{
    \left( \dfrac{s_1^2}{n_1} \right)^2
  }{(n_1-1)}
  +
  \dfrac{
    \left( \dfrac{s_2^2}{n_2} \right)^2
  }{(n_2-1)}
}
$$
:::


### Для связанных выборок {#ttest-twosample-paired}

$$
t = \frac{\overline X_D - \mu_0}{s_D / \sqrt{n}} \overset{H_0}{\thicksim} t(n-1)
$$

